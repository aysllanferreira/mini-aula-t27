// sort. every. find. foreach. filter. some. map. reduce. ||

const numeros = [1, 3, 7, 8, 9, 8, 10];
const palavras = ['palavras', 'apenas', 'pequenas', 'vento'];

const organiza = numeros.sort((a, b) => a - b);
const organiza2 = palavras.sort();

console.log(organiza2);