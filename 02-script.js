// sort. every. find. foreach. filter. some. map. reduce. ||

const numeros = [4, 2, 8, 7 , 4 , 13, 23, 12, 15];

const everys = numeros.every((elemento) => elemento < 14);
const somes = numeros.some((elemento) => elemento < 10);

// console.log(everys);
// console.log(somes);